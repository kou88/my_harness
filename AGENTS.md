# Agent Notes

## Compatibility Policy

互換性は、過去データ、外部契約、または稼働中クライアントに実際の価値がある場合だけ維持する。利用実績が少なく、過去データを保持する要件がない変更では、互換用の nullable 列、固定値、dual read/write、暗黙 fallback、旧経路を追加しない。旧データは migration で明示的に削除または変換し、現在の最良のモデルへ一本化する。互換性を残す場合は、保護する対象、終了条件、撤去手順を同じ変更内で明記する。
